{
  "name": "hiext-license-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Hiext license activation Worker (Cloudflare Workers + D1)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "db:init": "wrangler d1 execute hiext-license --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  }
}
